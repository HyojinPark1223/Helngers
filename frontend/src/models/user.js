export default class User {
  constructor(nickname, email, password, introduce) {
      this.nickname = nickname;
      this.email = email;
      this.password = password;
      this.introduce = introduce;
  }
}
